<UserControl x:Class="AniBased.View.Home.WindowAnimesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AniBased.View.Home"
             mc:Ignorable="d" 
             d:DesignHeight="758" d:DesignWidth="1515">
    <Grid>
        <Border Background="#1B263B"
                            BorderBrush="Transparent"
                            CornerRadius="16">
            <Grid>
                <ScrollViewer >
                    <!--<DataGrid Background="#1B263B" AutoGenerateColumns="True" ItemsSource="{Binding Posters}">
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <local:Poster Content="{Binding PosterVM}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid>-->
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
